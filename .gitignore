# never commit secrets or runtime artifacts
*.retry
*.vault
.vault_pass*
ansible/*.log

# real inventory holds host IP / user / key path — keep it local
ansible/inventory.ini

# Phase 2a secrets + private CA (never publish)
ansible/.vault_pass
ansible/certs/
ansible/group_vars/vps/vault.yml
